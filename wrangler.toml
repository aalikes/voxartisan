name = "voxartisan"
compatibility_date = "2025-07-12"
compatibility_flags = ["nodejs_compat"]
pages_build_output_dir = "."

# Workers AI — speech generation runs on Cloudflare's own models.
# No API key: the binding authenticates through the account itself.
[ai]
binding = "AI"

# D1 Database for speech storage
[[d1_databases]]
binding = "DB"
database_name = "voxartisan-db"
database_id = "ea9747bf-263d-47b2-9bda-94fc84381807"
