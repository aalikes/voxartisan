.env
.env.local
.DS_Store
*.sqlite
*.test.db
node_modules/

# Wrangler's local cache. Holds the Cloudflare account id and a connection
# trace with approximate location — not things to publish.
.wrangler/
